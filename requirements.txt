fastapi==0.116.1
uvicorn==0.35.0
starlette==0.47.3
httpx==0.28.1
python-dotenv==1.0.1
structlog==25.4.0
pydantic==2.11.9
pydantic-settings==2.6.1
kubernetes==30.1.0
prometheus-client==0.22.1
pyjwt[crypto]==2.8.0
jinja2==3.1.4
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
slack-sdk==3.30.0
aiohttp>=3.8.0

# Advanced NLP Dependencies (use environment markers for Python compatibility)
redis[hiredis]==5.0.1

# numpy wheels: 1.24.x doesn't support Python 3.13 -> use 2.1+ for 3.13, keep 1.24.3 otherwise
numpy==2.1.3; python_version >= "3.13"
numpy==1.24.3; python_version < "3.13"

# scikit-learn: wheels for 3.13 may not be available yet -> install only on <3.13
scikit-learn==1.3.0; python_version < "3.13"

nltk==3.8.1
# spaCy wheels for 3.13 may lag -> install only on <3.13
spacy==3.7.2; python_version < "3.13"

google-genai==0.8.0
anthropic==0.40.0
openai==1.51.0
fastmcp==0.1.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0

# NCP SDK
ncloud-sdk==1.1.6

# YAML parsing for partial manifest updates
pyyaml==6.0.1

# Optional (MCP integration) - safe to omit at first run
# fastapi-mcp>=0.1.0

